# Tensorboard CRD
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  name: tensorboards.tensorboard.kubeflow.org
spec:
  group: tensorboard.kubeflow.org
  names:
    kind: Tensorboard
    listKind: TensorboardList
    plural: tensorboards
    singular: tensorboard
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tensorboard is the Schema for the tensorboards API
        properties:
          apiVersion:
            description: "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values."
            type: string
          kind:
            description: "Kind is a string value representing the REST resource this object represents."
            type: string
          metadata:
            type: object
          spec:
            description: TensorboardSpec defines the desired state of Tensorboard
            properties:
              logspath:
                description: "Log path to the Tensorboard data"
                type: string
            required:
            - logspath
            type: object
          status:
            description: TensorboardStatus defines the observed state of Tensorboard
            properties:
              conditions:
                description: Conditions is an array of current conditions
                items:
                  description: TensorboardCondition defines the observed state of Tensorboard
                  properties:
                    deploymentState:
                      description: "Deployment status, 'Available', 'Progressing', 'ReplicaFailure'."
                      type: string
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                  required:
                  - deploymentState
                  type: object
                type: array
              readyReplicas:
                description: ReadyReplicas defines the number of Tensorboard Servers that are available to connect.
                format: int32
                type: integer
            required:
            - conditions
            - readyReplicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
